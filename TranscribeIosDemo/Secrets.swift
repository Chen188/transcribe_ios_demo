// Fill in your AWS credentials before building.
// NEVER commit real credentials to version control.
enum Secrets {
    static let accessKey = "YOUR_ACCESS_KEY_HERE"
    static let secretKey = "YOUR_SECRET_KEY_HERE"
}
